# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ecommerce_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf
spring.thymeleaf.cache=false

# Server
server.port=7111

# App name
spring.application.name=ecommerce

# ── Razorpay Payment Gateway (TEST mode) ──
# Get free TEST keys from https://dashboard.razorpay.com/app/keys and paste below.
# Leave them as REPLACE_ME to run in built-in DEMO mode (simulated payment, no keys needed).
razorpay.key.id=rzp_test_T5YE5bU3uNkclO
razorpay.key.secret=h99rjXtiH1AwOCMzu9yFuUdO
razorpay.currency=INR
